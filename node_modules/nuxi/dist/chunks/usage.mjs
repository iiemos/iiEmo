import { c as cyan } from '../shared/nuxi-edge.d36337ed.mjs';
import { s as showHelp } from '../shared/nuxi-edge.2542dd60.mjs';
import { d as defineNuxtCommand, c as commands } from '../shared/nuxi-edge.f39aafd9.mjs';
import 'tty';

const usage = defineNuxtCommand({
  meta: {
    name: "help",
    usage: "nuxt help",
    description: "Show help"
  },
  invoke(_args) {
    const sections = [];
    sections.push(`Usage: ${cyan(`npx nuxi ${Object.keys(commands).join("|")} [args]`)}`);
    console.log(sections.join("\n\n") + "\n");
    showHelp({});
  }
});

export { usage as default };
